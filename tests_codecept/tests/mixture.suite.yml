actor: MixtureTester
modules:
    enabled:
        - \Helper\Mixture
        - Asserts
        - WebDriver:
              browser: 'chrome'
              url: 'http://www.thinkapp.com'
        - Filesystem
    step_decorators: ~
paths:
    envs: tests/_envs